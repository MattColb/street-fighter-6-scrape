

def handler(event, context):
    #Take in User ID

    #Check if user exists within the DB
    #If not, create the user
    #If they do, get the latest date
    

    #Send 10 SQS queue messages corresponding to each page, as well as the last max time used. 
    #Could even have a prompting queue which acts based on a SQS cron job where you can turn users on and off depending on if they are playing

    pass